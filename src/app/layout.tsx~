import Header from '@/components/Header'
import Footer from '@/components/Footer'
import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import './globals.css'

// Fonts
const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
})
const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
})

// --- Metadata ---
export const metadata: Metadata = {
  title: {
    default: 'Praktisk assistanse – Finn Røsland',
    template: '%s | Praktisk assistanse',
  },
  description:
    'Praktisk assistanse tilbyr datahjelp, leksehjelp, øvingskøyring og praktisk støtte i kvardagen på Osterøy og i Bergen-området.',
  keywords: [
    'Praktisk assistanse',
    'Osterøy',
    'Bergen',
    'datahjelp',
    'leksehjelp',
    'øvingskøyring',
    'praktisk hjelp',
    'Finn Røsland',
  ],
  authors: [{ name: 'Finn Røsland' }],
  creator: 'Finn Røsland',
  openGraph: {
    title: 'Praktisk assistanse – Finn Røsland',
    description:
      'Hjelp til sjølvhjelp, øvingskøyring, leksehjelp og praktisk støtte i kvardagen på Osterøy og i Bergen-området.',
    url: 'https://praktiskassistanse.no',
    siteName: 'Praktisk assistanse',
    locale: 'nn_NO',
    type: 'website',
  },
  twitter: {
    card: 'summary_large_image',
    title: 'Praktisk assistanse – Finn Røsland',
    description:
      'Hjelp til sjølvhjelp, øvingskøyring, leksehjelp og praktisk støtte i kvardagen på Osterøy og i Bergen-området.',
  },
}

// --- Layout ---
export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="nn">
      <body
  			className={`${geistSans.variable} ${geistMono.variable} antialiased min-h-screen flex flex-col bg-brand-light text-neutral-dark`}
		>

        <Header />
			<main className="flex-1 max-w-5xl mx-auto w-full px-6 sm:px-8 lg:px-10 py-14 bg-white/90 backdrop-blur-md shadow-md rounded-3xl">

          {children}
        </main>
        <Footer />
      </body>
    </html>
  )
}
